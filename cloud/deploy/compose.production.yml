services:
  cloud:
    build:
      context: ..
      dockerfile: Dockerfile
    env_file:
      - ../.env
    environment:
      DIYA_HOST: 0.0.0.0
      DIYA_PORT: 8787
      DIYA_DATABASE_PATH: /data/diya-cloud.sqlite
    volumes:
      - diya-cloud-data:/data
    restart: unless-stopped
    networks: [diya]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8787/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3

  caddy:
    image: caddy:2.8-alpine
    environment:
      DIYA_DOMAIN: ${DIYA_DOMAIN}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped
    depends_on:
      cloud:
        condition: service_healthy
    networks: [diya]

volumes:
  diya-cloud-data:
  caddy-data:
  caddy-config:

networks:
  diya:
